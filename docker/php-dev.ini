; Настройки PHP для разработки на Windows.
;
; Код лежит на смонтированном с хоста томе, а обращение к нему идёт через
; прослойку Docker Desktop — один stat() файла стоит ~0.4-7 мс вместо
; микросекунд. Symfony в dev-режиме трогает около тысячи файлов за запрос,
; поэтому проверка изменений съедала несколько секунд на каждый ответ.

[opcache]
opcache.enable = 1
opcache.memory_consumption = 256
opcache.max_accelerated_files = 20000

; Главный рычаг: без этого PHP делает stat() каждого закэшированного файла
; на каждом запросе. Изменения в коде подхватываются раз в 30 секунд.
; Если правка не видна — docker compose restart php.
opcache.validate_timestamps = 1
opcache.revalidate_freq = 30

; Не проверять существование файла перед include: путь и так резолвится
; через realpath cache ниже.
opcache.enable_file_override = 0

; Второй уровень кэша — на диске контейнера (не на томе с хоста!).
; Память OPcache живёт до перезапуска php-fpm, а file_cache переживает его:
; после `docker compose restart php` воркеры поднимают байткод из
; /tmp/opcache вместо повторного парсинга ~1500 файлов с медленного тома.
; Каталог PHP создаёт сам при первой записи, /tmp доступен на запись всем —
; поэтому пересобирать образ ради mkdir не нужно.
opcache.file_cache = /tmp/opcache
opcache.file_cache_consistency_checks = 0

; Строки классов/методов Symfony и vendor занимают заметно больше, чем
; дефолтные 8M; при переполнении буфера они перестают переиспользоваться
; между воркерами и каждый платит за них своей памятью.
opcache.interned_strings_buffer = 32

[performance]
; Кэш разрешённых путей. Значения по умолчанию (16K / 120 c) не вмещают
; дерево vendor/, из-за чего пути резолвились заново на каждом запросе.
realpath_cache_size = 8192K
realpath_cache_ttl = 600
